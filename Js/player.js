const allPlayers = [];
function displayView(playerNumber){
    
}
function playerSelect(players)
{
    const playersName = players.parentNode.parentNode.children[0].innerText;

    const playerObj = {
        playersName
    }
    // console.log(playerObj)
    allPlayers.push(playerObj)
    // console.log(playerObj)
}